<UserControl x:Class="ModuleDialog.Views.MyDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModuleDialog.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800">
  <Grid Background="White" Height="350" Width="600">
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition Height="60"/>
    </Grid.RowDefinitions>

    <TextBlock
      Grid.Row="0"
      HorizontalAlignment="Center"
      VerticalAlignment="Center"
      Text="{Binding Content}"
      FontSize="50"
     />
    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
      <Button Content="Cancel" FontSize="20" Width="100" Command="{Binding CancelCommand}"/>
      <Button Content="Ok" FontSize="20" Width="100" Margin="10 0 10 0" Command="{Binding OkCommand}"/>
    </StackPanel>
  </Grid>
</UserControl>
